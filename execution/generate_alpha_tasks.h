#ifndef GENERATE_ALPHA_TASKS_H__
#define GENERATE_ALPHA_TASKS_H__

#include <tuple>
#include <string>

void generate_alpha_tasks(std::tuple <std::string> values);
void generate_alpha_tasks2(std::tuple <std::string> values);


#endif //GENERATE_ALPHA_TASKS_H__